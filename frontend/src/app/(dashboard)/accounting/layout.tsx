'use client';

/**
 * Project scope is controlled globally from the dashboard header (HeaderProjectSelect).
 */
export default function AccountingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
